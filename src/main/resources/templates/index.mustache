<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>확장자선택화면</title>
    <link rel="stylesheet" href="../css/style.css">
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0"/>
</head>
<body>
<section class="upload">
    <div style="height:30px; font-weight: bold;">
        업로드할 파일을 선택해주세요

    </div>
    <div class="wrap">
        <div class="selection-title">
            <span>파일첨부</span>
        </div>
        <div class="selection-format">
            <input id="file" type="file">
        </div>
        <div id="add-file">
            <button>+ 추가</button>
        </div>
    </div>
</section>
<section class="fixed-format">
    <div class="wrap">
        <div class="selection-title">
            <span>고정 확장자</span>
        </div>
        <div class="selection-format">
            {{#defaultExtensions}}
                <label>
                    <input class="default-file-extension" type="checkbox" name="fix-group" value={{name}} {{#isActive}}checked{{/isActive}}{{^isActive}}{{/isActive}}
                            data-id = {{id}}>
                    {{name}}
                </label>
            {{/defaultExtensions}}
        </div>
    </div>
</section>
<section class="custom-format">
    <div>
        <div class="wrap">
            <div class="selection-title">
                <span>커스텀 확장자</span>

            </div>
            <div class="selection-format">
                <div class="input-custom-format">
                    <div class="input-box">
                        <input id="extension-name" type="text" placeholder="확장자 입력">
                    </div>
                    <div id="add-button">
                        <button>+ 추가</button>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <div id="chip-area">
                <div>
                    <div id="count"> <div id="count-now">{{count}}</div>/200</div>
                </div>
                <div id="chip-wrap">
                    {{#customerExtensions}}
                        <div class="chip">
                            <div class="file-extension-name">{{name}}</div>
                            <span class="material-symbols-outlined" data-id={{id}}>
                            close
                        </span>
                        </div>
                    {{/customerExtensions}}
                </div>
            </div>
        </div>
    </div>
</section>
</body>
</html>
<script src="../js/nodeMaker.js"></script>
<script src="../js/postFile.js"></script>
<script src="../js/deleteExtension.js"></script>
<script src="../js/postExtension.js"></script>